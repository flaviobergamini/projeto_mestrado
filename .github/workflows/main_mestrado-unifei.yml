# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions
# More info on Python, GitHub Actions, and Azure App Service: https://aka.ms/python-webapps-actions

name: Build and deploy Python app to Azure Web App - mestrado-unifei

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python version
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      # 🛠️ Local Build Section (Optional)
      # The following section in your workflow is designed to catch build issues early on the client side, before deployment. This can be helpful for debugging and validation. However, if this step significantly increases deployment time and early detection is not critical for your workflow, you may remove this section to streamline the deployment process.
      - name: Create and Start virtual environment and Install dependencies
        run: |
          python -m venv antenv
          source antenv/bin/activate
          pip install -r requirements.txt
                
      # By default, when you enable GitHub CI/CD integration through the Azure portal, the platform automatically sets the SCM_DO_BUILD_DURING_DEPLOYMENT application setting to true. This triggers the use of Oryx, a build engine that handles application compilation and dependency installation (e.g., pip install) directly on the platform during deployment. Hence, we exclude the antenv virtual environment directory from the deployment artifact to reduce the payload size.

      - name: Clean unnecessary files to reduce artifact size
        run: |
          # Remove arquivos desnecessários para reduzir drasticamente o tamanho
          rm -rf .git .github .vscode __pycache__ .pytest_cache alembic
          find . -type d -name "__pycache__" -exec rm -rf {} + || true
          find . -type f -name "*.pyc" -delete || true
          find . -type f -name "*.pyo" -delete || true
          find . -type f -name "*.log" -delete || true
          rm -f requirements.txt.backup || true

      - name: Upload artifact for deployment jobs
        uses: actions/upload-artifact@v4
        with:
          name: python-app
          path: |
            .
            !antenv/
            !.git/
            !.github/
            !.vscode/
            !__pycache__/
            !*.pyc
            !*.pyo
            !*.log
            !.env
            !.env.*
            !alembic/
            !requirements.txt.backup

      # 🚫 Opting Out of Oryx Build
      # If you prefer to disable the Oryx build process during deployment, follow these steps:
      # 1. Remove the SCM_DO_BUILD_DURING_DEPLOYMENT app setting from your Azure App Service Environment variables.
      # 2. Refer to sample workflows for alternative deployment strategies: https://github.com/Azure/actions-workflow-samples/tree/master/AppService
      

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write #This is required for requesting the JWT
      contents: read #This is required for actions/checkout

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: python-app
      
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_0C3D68968A824847B39A20516318616B }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_D41D07E865FB4A05BD84B6D2307A4725 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_765FAC8CDB6A4CBD8078642EA0C4074D }}

      - name: Get Resource Group and Stop App Service
        run: |
          echo "🔍 Descobrindo o Resource Group..."
          RESOURCE_GROUP=$(az webapp show --name mestrado-unifei --query resourceGroup -o tsv)
          echo "📦 Resource Group encontrado: $RESOURCE_GROUP"

          echo "⏹️ Parando o App Service..."
          az webapp stop --name mestrado-unifei --resource-group $RESOURCE_GROUP

          echo "⏳ Aguardando 15 segundos para garantir que parou..."
          sleep 15

          echo "✅ App Service parado com sucesso!"
        continue-on-error: false

      - name: 'Deploy to Azure Web App'
        uses: azure/webapps-deploy@v3
        id: deploy-to-webapp
        with:
          app-name: 'mestrado-unifei'
          slot-name: 'Production'
          package: .
          clean: true

      - name: Start App Service
        run: |
          echo "🔍 Descobrindo o Resource Group..."
          RESOURCE_GROUP=$(az webapp show --name mestrado-unifei --query resourceGroup -o tsv)

          echo "▶️ Iniciando o App Service..."
          az webapp start --name mestrado-unifei --resource-group $RESOURCE_GROUP

          echo "✅ App Service iniciado com sucesso!"
          echo "🌐 URL: https://mestrado-unifei-fzg4awcvb6cxb3eb.brazilsouth-01.azurewebsites.net"
        continue-on-error: false